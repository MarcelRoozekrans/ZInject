name: Publish to NuGet

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.0.x
            9.0.x
            10.0.x

      - name: Install GitVersion
        uses: gittools/actions/gitversion/setup@v3
        with:
          versionSpec: '6.x'

      - name: Determine version
        id: gitversion
        uses: gittools/actions/gitversion/execute@v3

      - name: Build
        run: dotnet build ZInject.slnx --configuration Release -p:Version=${{ steps.gitversion.outputs.semVer }}

      - name: Test
        run: dotnet test tests/ZInject.Tests/ZInject.Tests.csproj --configuration Release --no-build --verbosity normal

      - name: Pack
        run: dotnet pack ZInject.slnx --configuration Release --no-build --output ./artifacts -p:Version=${{ steps.gitversion.outputs.semVer }}

      - name: Push to NuGet
        run: dotnet nuget push ./artifacts/*.nupkg --source https://api.nuget.org/v3/index.json --api-key ${{ secrets.NUGET_API_KEY }} --skip-duplicate

      - name: Push to GitHub Packages
        run: dotnet nuget push ./artifacts/*.nupkg --source https://nuget.pkg.github.com/MarcelRoozekrans/index.json --api-key ${{ secrets.GITHUB_TOKEN }} --skip-duplicate
